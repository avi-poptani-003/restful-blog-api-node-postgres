{
  "name": "blog_api",
  "version": "1.0.0",
  "description": "This is a simple Blog API. This API provides CRUD (Create, Read, Update, Delete) operations for managing blog posts stored in a PostgreSQL database. It allows users to retrieve all posts, fetch individual posts by ID, create new posts, update existing posts, and delete posts through RESTful endpoints. ",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    
  },
  "author": "Avi Poptani",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
